"""Integration suite — real Postgres, real object store.

Everything here needs infrastructure and skips without it. The unit suites
prove the logic; these prove the adapters behave the way the fakes promised.
"""
